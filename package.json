{
  "name": "aki-intervals",
  "version": "1.0.0",
  "private": true,
  "description": "Aki — a mocha Japandi sprint, interval & breathing timer (Capacitor iOS wrapper)",
  "scripts": {
    "build:web": "node scripts/copy-web.mjs",
    "ios:add": "npm run build:web && npx cap add ios",
    "sync": "npm run build:web && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
